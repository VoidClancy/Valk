package {{ .PackageName }}

import (
	"context"
	{{- if hasJsonField .Model }}
	"encoding/json"
	{{- end }}
	"fmt"
	"slices"
	{{- if hasTimeField .Model }}
	"time"
	{{- end }}
	{{- if ne .PackageName .ParentPackageName }}
	"{{ .ParentImportPath }}"
	{{- end }}
)

