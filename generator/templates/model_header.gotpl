package {{ .PackageName }}

import (
	"context"
	"database/sql"
	{{- if hasModelType .Model "Json" }}
	"encoding/json"
	{{- end }}
	"errors"
	"fmt"
	"slices"
	"strings"
	{{- if hasModelType .Model "DateTime" }}
	"time"
	{{- end }}
	{{- if and (hasModelType .Model "Array") (or (eq .Schema.Datasource.Provider "postgres") (eq .Schema.Datasource.Provider "postgresql")) }}
	"github.com/lib/pq"
	{{- end }}
	{{- if ne .PackageName .ParentPackageName }}
	"{{ .ParentImportPath }}"
	{{- end }}
)

{{- if and (hasModelType .Model "Array") (or (eq .Schema.Datasource.Provider "postgres") (eq .Schema.Datasource.Provider "postgresql")) }}
var _ = pq.Array
{{- end }}

