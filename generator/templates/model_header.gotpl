package {{ .PackageName }}

import (
	"context"
	"database/sql"
	{{- if hasJsonField .Model }}
	"encoding/json"
	{{- end }}
	"fmt"
	"slices"
	"strings"
	{{- if hasTimeField .Model }}
	"time"
	{{- end }}
	{{- if ne .PackageName .ParentPackageName }}
	"{{ .ParentImportPath }}"
	{{- end }}
)

